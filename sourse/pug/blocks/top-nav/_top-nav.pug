mixin mob-mnu()
	-
		//- let path = require('path');
		//- let fs = require('fs');
		//- var requestURL = 'top-nav.json';
		//- var request = new XMLHttpRequest();
		//- request.open('GET', requestURL);
		//- request.responseType = 'json';
		//- let dataJson = JSON.parse(json);
	//- h1= dataJson['lala'] 
	+b.toggle-menu-mobile--js&attributes(attributes)
		span
	+b.menu-mobile--js&attributes(attributes)
		+b.toggle-menu-mobile--js
			span
		+e.inner
			+mnu-sm()
			+mnu()
			+e.buttons
				+e.lk(href="#")
					+icon('user')
					span Личный  кабинет
				+e.logout(href="#")
					+icon('logout')
					span Выйти
				+e.basket(href="#")
					+icon('shopping-cart')
					+e.quantity 0
				+e.btn.btn.btn-dark(href="#") 
					span Нужна консультация?
			.text-right
	//  мобильное меню
mixin top-nav() 
	// start top-nav
	+b.top-nav.block-with-lazy&attributes(attributes)
		+mnu-sm(class="mnu-sm")
		+mnu()

					
	// end top-nav